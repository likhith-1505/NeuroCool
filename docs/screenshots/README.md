# Screenshots

Captures used by the root [`README.md`](../../README.md).

| File | Page | Shows |
| --- | --- | --- |
| `mission-control.png` | `/mission-control` | Metrics ribbon, rack field, NeuroCore recommendation, event timeline |
| `digital-twin.png` | `/digital-twin` | Rack inspector with an at-risk rack during a cooling-failure scenario |
| `analytics.png` | `/analytics` | Cluster thermal forecast curve + efficiency / energy / accuracy gauges |
| `analytics-forecast.png` | `/analytics` | Per-rack health-forecast bands + generated insight |
| `ai-copilot.png` | `/ai-copilot` | Streaming reasoning console with a grounded multi-recommendation answer |
| `settings.png` | `/settings` | Appearance, motion, prediction interval, simulation mode, AI provider |

To refresh: run the stack, open the app, press **Start** in the simulation dock, let it
run ~30–60s, trigger a scenario (<kbd>Ctrl</kbd>/<kbd>Cmd</kbd>+<kbd>K</kbd>) where an
at-risk rack helps, then capture at ~1600px wide.
