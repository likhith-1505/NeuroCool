"""Explicit, typed tools the NeuroCore LLM tool-calling loop may invoke —
see .base for the Tool contract, .registry for the fixed set, and
.executor for where every call is validated/authorized/dispatched.
"""
